# PostgreSQL connection string used by the booking & contact API routes.
# Required only when submitting bookings/messages — the site renders without it.
DATABASE_URL=postgresql://user:password@localhost:5432/pet_care
